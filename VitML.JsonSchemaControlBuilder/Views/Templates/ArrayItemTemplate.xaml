<UserControl x:Class="MyVisualJSONEditor.Views.Templates.ArrayItemTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
             xmlns:mtkConv="clr-namespace:MyToolkit.Converters;assembly=MyToolkit.Extended"
             xmlns:myConv="clr-namespace:MyVisualJSONEditor.Views.Converters"
             xmlns:views="clr-namespace:MyVisualJSONEditor.Views" 
             mc:Ignorable="d" 
             d:DesignWidth="300">
    <Grid Margin="4,0,4,0" Background="WhiteSmoke" >
        <GroupBox Header="Item">
            <views:JsonSchemaBuilder Data="{Binding}" Margin="4,8,4,0" />
        </GroupBox>
        <Button Content="Remove" HorizontalAlignment="Right" 
                MinWidth="75" Height="23" VerticalAlignment="Top"
                Click="OnRemoveArrayObject" 
                Tag="{Binding}" Margin="4,0,12,4" />
    </Grid>
</UserControl>
